spring:
  application:
    name: vivara-kendram
  
  datasource:
    url: jdbc:postgresql://localhost:5432/vivara-kendram
    username: postgres
    password: root

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
